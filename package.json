{
  "name": "image-filter-benchmark",
  "version": "1.0.0",
  "description": "Benchmark comparing JavaScript vs WebAssembly for image filter processing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-wasm": "cd wasm-version && chmod +x build.sh && ./build.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webassembly",
    "wasm",
    "image-processing",
    "benchmark",
    "performance"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
